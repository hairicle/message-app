'use client';

export default function ChatPage() {
  return (
    <div className="flex h-screen" style={{ background: 'var(--bg)' }}>
      <p className="m-auto text-sm" style={{ color: 'var(--text-dim)' }}>
        Migrate ChatPage from frontend/src/pages/ChatPage.tsx
      </p>
    </div>
  );
}
